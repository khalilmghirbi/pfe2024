// helpers/dateHelper.js

// Fonction pour formater la date en format de base de données
const dateToDbFormat = (date) => {
    // Implémentez votre logique pour formater la date ici
    return date;
  };
  
  module.exports = {
    dateToDbFormat,
  };
  